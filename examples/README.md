# Example Content Packs

Example packs show how to adapt the engine to a subject. The current pack is `thai-stem-role-models`.

Use a separate folder for each topic. Keep content-specific facts and assets in the pack; keep game logic in `app/`.
